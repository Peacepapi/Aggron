<div class= "well">
	<p>
		<strong>Commenter: </strong><%= link_to comment.commenter.username, user_path(comment.commenter) %>
		<br>
		<%= comment.body %>
	</p>

	<p>
		<span><%= time_ago_in_words(comment.created_at) %></span>
	</p>
</div>
